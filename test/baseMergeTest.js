import chai from "chai"
// import file from "../src/.internal/baseMerge.js"
import { assert } from "chai"

// if using older versions of node
// var chai = require("chai")
// var file = require ("../src/add")

const expect = chai.expect


describe("baseMergeTest", () => {
    /*
    def function customizer (array){
        TODO: Function to customize merged values
        returns cuztomized merge
    }
    it("Checks if merge source is empty and merge works", () =>{
        expect(file(object, source, srcIndex, customizer, stack)).to.equal(Merged_object);
    });
    it("Checks if object is empty and merge works", () =>{
        expect(file(object, source, srcIndex, customizer, stack)).to.equal(Merged_object);
    });
    it("Checks if object is invalid", () =>{
        expect(() => file(object, source, srcIndex, customizer, stack))
        .to.throw(Error), 'object is invalid'    
    });
    it("Checks if source is invalid", () =>{
        expect(() => file(object, source, srcIndex, customizer, stack))
        .to.throw(Error), 'source is invalid'        
    });
    it("Checks if srcIndex is invalid", () =>{
        expect(() => file(object, source, srcIndex, customizer, stack))
        .to.throw(Error), 'srcIndex is invalid'    
    });
    it("Checks if stack is invalid", () =>{
        expect(() => file(object, source, srcIndex, customizer, stack))
        .to.throw(Error), 'stack is invalid'
    });
    it("Checks if merge works with simple values", () =>{
        expect(file(object, source, srcIndex, customizer, stack)).to.equal(Merged_object);
    });
    it("Checks if merge works with complicated values", () =>{
        expect(file(object, source, srcIndex, customizer, stack)).to.equal(Merged_object);
    });
    */
})