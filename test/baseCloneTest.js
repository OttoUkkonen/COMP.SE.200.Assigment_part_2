import chai from "chai"



// if using older versions of node
// var chai = require("chai")
// var file = require ("../src/add")


const expect = chai.expect


describe("baseCloneTest", () => {
    /*
    def function customizer (customizer){
        TODO: Function for customize cloning
        returns customation
    }
    it("Cloning works with correct variables and returns the clone", () =>{
        expect(file(value, bitmask, customizer, key, object, stack)
        .to.equal(clone);
    });
    it("Cloning with erroneus value", () =>{
        expect(() => file(value, bitmask, customizer, key, object, stack))
        .to.throw(Error), 'value is faulty'
    });
    it("Cloning with erroneus bitmask flag", () =>{
        expect(() => file(value, bitmask, customizer, key, object, stack))
        .to.throw(Error), 'bitmask is faulty'
    });
    it("Cloning with erroneus key", () =>{
        expect(() => file(value, bitmask, customizer, key, object, stack))
        .to.throw(Error), 'key is faulty'
    });
    it("Cloning with erroneus object", () =>{
        expect(() => file(value, bitmask, customizer, key, object, stack))
        .to.throw(Error), 'object is faulty'
    });
    it("Cloning with erroneus stack", () =>{
        expect(() => file(value, bitmask, customizer, key, object, stack))
        .to.throw(Error), 'stack is faulty'
    });
    */
})

