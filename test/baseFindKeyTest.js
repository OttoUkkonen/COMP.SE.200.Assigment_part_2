import chai from "chai"
import file from "../src/.internal/baseFindKey.js"
import { assert } from "chai"

// if using older versions of node
// var chai = require("chai")
// var file = require ("../src/add")


const expect = chai.expect


describe("baseFindKeyTest", () => {
    /*
    def function predicate (collection){
        TODO: Function to invoked per iteration
        returns boolean
    }
    def function eachFunc (collection){
        TODO: Function to iterate over 'collection'
        returns iterations one by one
    }
    it("baseFindKey works with correct and matching elemnt and returns the key of element", () =>{
        expect(file(collection, predicate, eachFunc)).to.equal(key);
    });
    it("baseFindKey works with correct but not matching variables and returns the undefined", () =>{
        expect(file(collection, predicate, eachFunc)).to.equal(undefined);
    });
    it("baseFindKey with erroneus collection", () =>{
        expect(() => file(collection, predicate, eachFunc))
        .to.throw(Error), 'collection is faulty'
    });
    */
})

