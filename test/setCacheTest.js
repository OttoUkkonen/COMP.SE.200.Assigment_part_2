import chai from "chai"
import file from "../src/.internal/SetCache.js"
import { assert } from "chai"

// if using older versions of node
// var chai = require("chai")
// var file = require ("../src/add")


const expect = chai.expect


describe("setCacheTest", () => {
    /*
    it("Checks if constructor works", () =>{
        expect(file(values)).to.equal(setCache_object);
    });
    it("Creating class setCache with erroneus values", () =>{
        expect(() => file(values)).to.throw(Error), 'values is faulty'
    });
    it("Checks if class setCache has function works, value is in setCache", () =>{
        expect(file(values).has(value)).to.equal(true);
    });
    it("Checks if class setCache has function works, value is not in setCache", () =>{
        expect(file(values).has(value)).to.equal(false);
    });
    it("Checks if class setCache add function works", () =>{
        expect(file(values).add(value)).to.equal(setCache_cache_instance);
    });
    it("Checks if class setCache has function works, with erroneus value", () =>{
        expect(() => file(values)).has(value).to.throw(Error), 'value is faulty'
    });
    it("Checks if class setCache add function works, with erroneus value", () =>{
        expect(() => file(values)).add(value).to.throw(Error), 'value is faulty'
    });
    */
})

