import chai, {Assertion, Assertion} from "chai"
import file from "../src/.internal/addAssignValue.js"

// var chai = require("chai")
// var file = require ("../src/add")
// testing branch

const expect = chai.expect

describe("addAssignValueTest", () => {

	it("Assign key value pair, checks if the existing value is not equivalent")
		function assignValue (object, key, value) {
		const object = object[key]

        expect (value == value[key])
		if(!(hasOwnProperty.call(object, key)&&(objectValue, value))){
			if(value !== 0 || (1 / value) == (1 /objectValue, value))
				baseAssignValue(object,key, value)
				baseAssignValue(object, key, value)
        if (value == undefined && !(key in object)){
                baseAssignValue(object, key , value)
		};
           

	}
}   

});

	