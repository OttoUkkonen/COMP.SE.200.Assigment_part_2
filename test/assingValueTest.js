import chai from "chai"
import file from "../src/.internal/assignValue.js"
// for old node versions
// var chai = require("chai")
// var file = require ("../src/add")


const expect = chai.expect



describe("AssignValueTest", () => {
    /*
    it("Checks if existing key value changes", () =>{
        expect(file(object,key,value).get(key)).to.equal(value);
    });
    it("Test if key is not in object", () =>{
        expect(file(object,key,value).get(key)).to.equal(value);
    });
    it("Test if key's values stays the same in object, if value was the original", () =>{
        expect(file(object,key,value).get(key)).to.equal(value);
    });
    it("Test when object is faulty", () =>{
        expect(() => file(object,key,value)).to.throw(Error), 'object is faulty'
    });
    it("Test when value is faulty", () =>{
        expect(() => file(object,key,value)).to.throw(Error), 'value is faulty'
    });
    it("Test when key is faulty", () =>{
        expect(() => file(object,key,value)).to.throw(Error), 'key is faulty'
    });
    */
})

