import chai from "chai"
import file from "../src/.internal/Hash.js"
import { assert } from "chai"

// if using older versions of node
// var chai = require("chai")
// var file = require ("../src/add")


const expect = chai.expect


describe("hashTest", () => {
    /*
    it("Checks if constructor works", () =>{
        expect(file(entries)).to.equal(hash_object);
    });
    it("Creating hash with erroneus entries", () =>{
         expect(() => file(entries)).to.throw(Error), 'object is faulty'
    });
    it("Checks if class hash clear function works", () =>{
        expect(file(entries).clear).to.equal(cleared_hash);
    });
    it("Checks if class hash get function works", () =>{
        expect(file(entries).get(key)).to.equal(key_entry_value);
    });
    it("Checks if class hash get function works with erroneus key", () =>{
        expect(() => file(entries)).get(key).to.throw(Error), 'key is faulty'
    });
    it("Checks if class hash delete function works, key is in hash", () =>{
        expect(file(entries).delete(key)).to.equal(true);
    });
    it("Checks if class hash delete function works, key is not in hash", () =>{
        expect(file(entries).delete(key)).to.equal(false);
    });
    it("Checks if class hash delete function works with erroneus key", () =>{
        expect(() => file(entries)).delete(key).to.throw(Error), 'key is faulty'
    });
    it("Checks if class hash has function works, key is in hash", () =>{
        expect(file(entries).has(key)).to.equal(true);
    });
    it("Checks if class hash has function works, key is not in hash", () =>{
        expect(file(entries).has(key)).to.equal(false);
    });
    it("Checks if class hash has function works with erroneus key", () =>{
        expect(() => file(entries)).has(key).to.throw(Error), 'key is faulty'
    });
    it("Checks if class hash set function works, with correct variables", () =>{
        expect(file(entries).set(key,value)).to.equal(hash_with_hash_instance);
    });
    it("Checks if class hash set function works, with erroneus key", () =>{
        expect(() => file(entries)).set(key,value).to.throw(Error), 'key is faulty'
    });
    it("Checks if class hash set function works, with erroneus value", () =>{
        expect(() => file(entries)).set(key,value).to.throw(Error), 'value is faulty'
    });
    */
})

