import chai from "chai"
import file from "../src/.internal/Stack.js"
import { assert } from "chai"

// if using older versions of node
// var chai = require("chai")
// var file = require ("../src/add")


const expect = chai.expect


describe("stackTest", () => {
    /*
    it("Checks if constructor works", () =>{
        expect(file(entries)).to.equal(Stack_object);
    });
    it("Creating class Stack with erroneus entries", () =>{
        expect(() => file(entries)).to.throw(Error), 'entries is faulty'
    });
    it("Checks if class Stack clear function works", () =>{
        expect(file(entries).clear).to.equal(cleared_Stack);
    });
    it("Checks if class Stack delete function works, key is in Stack", () =>{
        expect(file(entries).delete(key)).to.equal(true);
    });
    it("Checks if class Stack delete function works, key is not in Stack", () =>{
        expect(file(entries).delete(key)).to.equal(false);
    });
    it("Checks if class Stack delete function works with erroneus key", () =>{
        expect(() => file(entries)).delete(key).to.throw(Error), 'key is faulty'
    });


    it("Checks if class Stack get function works", () =>{
        expect(file(entries).get(key)).to.equal(key_entry_value);
    });
    it("Checks if class Stack get function works, with erroneus key", () =>{
        expect(() => file(entries)).get(key).to.throw(Error), 'key is faulty'
    });

    it("Checks if class Stack has function works, key is in Stack", () =>{
        expect(file(entries).has(key)).to.equal(true);
    });
    it("Checks if class Stack has function works, key is not in Stack", () =>{
        expect(file(entries).has(key)).to.equal(false);
    });
    it("Checks if class Stack has function works with erroneus key", () =>{
         expect(() => file(entries).has(key)).to.throw(Error), 'key is faulty'
    });

    it("Checks if class Stack set function works, with correct variables", () =>{
        expect(file(entries).set(key,value)).to.equal(Stack_with_cache_instance);
    });
    it("Checks if class Stack set function works, with erroneus key", () =>{
        expect(() => file(entries)).set(key,value).to.throw(Error), 'key is faulty'
    });
    it("Checks if class Stack set function works, with erroneus value", () =>{
        expect(() => file(entries)).set(key,value).to.throw(Error), 'value is faulty'
    });
    */
})

